# ============================================
# CONFIGURAÇÃO DO AMBIENTE - API ATENA
# ============================================
# 
# INSTRUÇÕES:
# 1. Copie este arquivo para .env: cp .env.example .env
# 2. Preencha os valores abaixo com suas credenciais reais
# 3. NUNCA commite o arquivo .env no Git!
#
# ============================================

# --------------------------------------------
# CONFIGURAÇÕES DA APLICAÇÃO
# --------------------------------------------
PROJECT_NAME=API de Correção de Redação ENEM
VERSION=1.0.0

# --------------------------------------------
# AUTENTICAÇÃO JWT
# --------------------------------------------
# Gere uma chave secreta segura com:
# python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=SUA_CHAVE_SECRETA_AQUI_GERE_UMA_CHAVE_FORTE_E_SEGURA
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# --------------------------------------------
# BANCO DE DADOS POSTGRESQL
# --------------------------------------------
# Para Docker Compose (desenvolvimento):
DATABASE_URL=postgresql://admin:supersecret@db:5432/enem_correcoes_db

# Para produção (VPS), use a URL completa:
# DATABASE_URL=postgresql://usuario:senha@localhost:5432/enem_correcoes_db

POSTGRES_USER=admin
POSTGRES_PASSWORD=SUA_SENHA_POSTGRES_AQUI
POSTGRES_DB=enem_correcoes_db

# --------------------------------------------
# CELERY / REDIS (FILAS DE PROCESSAMENTO)
# --------------------------------------------
# Para Docker Compose (desenvolvimento):
CELERY_BROKER_URL=redis://redis:6379/0
CELERY_RESULT_BACKEND=redis://redis:6379/0

# Para produção (VPS), se Redis estiver em outro servidor:
# CELERY_BROKER_URL=redis://localhost:6379/0
# CELERY_RESULT_BACKEND=redis://localhost:6379/0

# --------------------------------------------
# INTEGRAÇÃO COM LLM (LARGE LANGUAGE MODEL)
# --------------------------------------------
# Google Gemini (recomendado)
GOOGLE_API_KEY=SUA_GOOGLE_API_KEY_AQUI
LLM_PROVIDER=gemini
LLM_MODEL=models/gemini-1.5-flash

# Perplexity (alternativa - opcional)
# PPLX_API_KEY=SUA_PERPLEXITY_API_KEY_AQUI

# ============================================
# NOTAS IMPORTANTES:
# ============================================
# 
# 1. SECRET_KEY: Deve ser uma string longa e aleatória
#    - Use o comando Python acima para gerar
#    - NUNCA compartilhe esta chave
#    - Em produção, use variáveis de ambiente do sistema
#
# 2. GOOGLE_API_KEY: Obtenha em:
#    https://makersuite.google.com/app/apikey
#
# 3. POSTGRES_PASSWORD: Use uma senha forte
#    - Mínimo 16 caracteres
#    - Combine letras, números e símbolos
#
# 4. Em produção, considere usar um gerenciador
#    de segredos (ex: HashiCorp Vault, AWS Secrets Manager)
#
# ============================================
